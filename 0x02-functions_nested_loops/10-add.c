#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @i: the first parameter/argument
 * @j: the second parameter/argument
 * Return: the value of the result
 */

int add(int i, int j)
{
	int k;

	i = 89;
	j = 9;
	k = 89 + 9;

	if (i + j)
	{
	return (k);
	}
	return (0);
}

