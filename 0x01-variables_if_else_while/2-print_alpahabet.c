#include <stdio.h>
/**
 * main - a program that prints the alphabet in lowercase
 *
 * Return: 0 (Success)

int main(void)
{
	char a;
	char z;

	while (a < z)
	{
		putchar(a);
		a++;
	}
	putchar("\n");
	return (0)
}
